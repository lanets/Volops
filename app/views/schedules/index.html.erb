<% content_for :top_content do %>
  <ol class="breadcrumb">
    <li class="breadcrumb-item">
      <%= link_to @event.title, event_path(@event) %>
    </li>
    <li class="breadcrumb-item active">Schedule</li>
  </ol>
  <div class="row">
    <div class="col-12">
      <h1>Schedule</h1>
      <hr>
      <%= react_component('Scheduler', {schedule: @schedule}) %>
    </div>
  </div>
<% end %>